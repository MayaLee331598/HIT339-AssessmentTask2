@model IEnumerable<AssessmentTask2.Models.Duration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TimeTaken)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TimeTaken)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DurationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DurationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DurationId })
        </td>
    </tr>
}

</table>
